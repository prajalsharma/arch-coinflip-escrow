# ---- Settlement backend ----
# The ONLY secret in this system. Authorizes every payout.
# Get it with:
#   python3 -c "import json;print(bytes(json.load(open('program/.testnet-authority.json'))).hex())"
HOUSE_AUTHORITY_SECRET_KEY=

# Local dev alternative (path instead of inline hex):
# HOUSE_AUTHORITY_KEY_FILE=./program/.testnet-authority.json

# Deployed program (64-char hex)
PROGRAM_ID=e2c42f6caec4783e4573085e10c7125edaf182fda4b0f8cbb96f17ae72a141c4

# testnet | localnet
ARCH_NETWORK=testnet
PORT=8080

# Optional overrides (sane testnet defaults are built in)
# ARCH_RPC_URL=https://rpc.testnet.arch.network
# ARCH_TITAN_URL=https://titan.testnet.arch.network
